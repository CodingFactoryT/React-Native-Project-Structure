Main.tsx is called by App.tsx and handles the navigation of the app,
or in other words, which screen should be rendered. 

Example:
Main.tsx could check if the user is logged in.
If he is, Main.tsx renders the Home Screen
If not, Main.tsx renders the login page